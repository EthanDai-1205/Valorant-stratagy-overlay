use crate::state::*;

// OCR placeholder implementation
pub fn extract_text(frame: &[u8]) -> String {
    // OCR will be added back later
    String::new()
}

pub fn extract_ability_cooldowns(frame: &[u8]) -> Vec<u32> {
    // OCR will be added back later
    Vec::new()
}

